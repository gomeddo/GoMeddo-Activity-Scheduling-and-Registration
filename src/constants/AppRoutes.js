// This object defines the routes of the web application.
const AppRoutes = {
    // The root route of the application, representing the homepage.
    root: "/",

    // The route for the dashboard page.
    dashboard: "/dashboard",

    // The route for the booking page.
    booking: "/booking",
};

// Exporting the AppRoutes object to make it accessible to other modules.
export default AppRoutes;
